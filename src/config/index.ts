import { robloxConfig } from "./robloxConfig";
import { emailConfig } from "./emailConfig";
import { serverConfig } from "./serverConfig";
import { googleConfig } from "./googleConfig";

export const config = {
    serverConfig,
    googleConfig,
    emailConfig,
    robloxConfig,
};